# ─── Web server ──────────────────────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.6
httpx==0.27.0
python-dotenv==1.0.1

# ─── PDF processing ──────────────────────────────────────────────────────────
pymupdf==1.24.10          # PDF → images, text, compress
pikepdf==9.3.0            # PDF unlock, encrypt, repair, split, merge
pdfplumber==0.11.4        # PDF → Excel (table extraction)
weasyprint==62.3          # HTML → PDF

# ─── Image processing ────────────────────────────────────────────────────────
Pillow==10.4.0            # Image conversion, resize, grayscale
pillow-heif==0.18.0       # HEIC / HEIF (iPhone photos)
cairosvg==2.7.1           # SVG → PNG / PDF
rembg==2.0.57             # AI background removal (offline)

# ─── Audio / Video ───────────────────────────────────────────────────────────
openai==1.40.0            # Whisper API for transcription
pydub==0.25.1             # Audio chunking
ffmpeg-python==0.2.0      # FFmpeg wrapper for video → audio extraction

# ─── Utilities ───────────────────────────────────────────────────────────────
python-multipart==0.0.9
boto3==1.34.160           # For R2 (S3-compatible) — used when STORAGE_DRIVER=r2
